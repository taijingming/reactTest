#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_autod@3.1.0@autod/bin/autod.js" "$@"
  ret=$?
else 
  node  "$basedir/../_autod@3.1.0@autod/bin/autod.js" "$@"
  ret=$?
fi
exit $ret
